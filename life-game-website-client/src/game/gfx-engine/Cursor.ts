import defaultCursor from './default_cursor.png';
import targetCursor from './target_cursor.png';
import selectCursor from './select_cursor.png';

export enum Cursor {
  DEFAULT = 'DEFAULT',
  SELECT = 'SELECT',
  TARGET = 'TARGET',
}

export { defaultCursor, targetCursor, selectCursor };
