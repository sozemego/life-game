import React from 'react';

const style = {
  borderBottom: '1px solid gray',
  height: '1px',
  width: '100%'
};

const Divider = props => {
  return <div style={style} />;
};

export default Divider;
